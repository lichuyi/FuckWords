# Love U

一个极简的 GitHub Pages 静态页面项目。

## 在线访问

页面地址：https://lichuyi.github.io/FuckWords/

## 部署方式

1. 把本项目推送到 GitHub 仓库。
2. 进入仓库的 `Settings` > `Pages`。
3. 在 `Build and deployment` 中选择 `Deploy from a branch`。
4. 选择 `gh-pages` 分支和 `/root` 目录，然后保存。

GitHub Pages 发布后会自动使用 `index.html` 作为首页。
